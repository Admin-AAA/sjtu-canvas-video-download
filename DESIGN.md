# SJTU Canvas 视频下载器 · 界面设计方案

## 设计方向
干净、克制、接近 macOS 原生质感的单窗口工具。左侧深色侧边栏 + 浅色内容画布 + 白色卡片，单一蓝色主行动色，大量留白，清晰的字体层级，克制的 hover / 选中态。**界面上不出现任何 V1 / V2 等版本字样。**

## 设计令牌
| 类别 | 取值 |
| --- | --- |
| 画布背景 | `#F4F6FA` |
| 卡片 / 面板 | `#FFFFFF` |
| 次级面板 | `#F0F3F9` |
| 主文字 | `#1A2030` |
| 次要文字 | `#707A8A` |
| 描边 | `#E4E8F0` |
| 主色 | `#3B6FF5`（hover `#2E5BE0`，禁用 `#B9C6F7`，浅底 `#EAF0FE`） |
| 侧边栏底 | `#131A26`，文字 `#AEB9CC`，hover `#1E2735` |
| 语义色 | 成功 `#16A34A` / 警告 `#D97706` / 危险 `#DC2626` |

字体（PingFang SC 优先）：11 / 12 / 13 / 16 / 20，标题加粗。

间距：4px 基准。卡片内边距 20×18，区块间距 14，页面左右留白 28。

## 组件规范
- **侧边栏**：顶部 App Logo（圆角方块 + 字母），导航项整行高亮胶囊（浅蓝底 + 蓝字 + 3px 左侧主色条），底部仅留 App 名称。
- **按钮**：主按钮（蓝底白字，hover 加深，禁用变浅）；次按钮（白底描边，hover 浅底）。
- **输入 / 下拉**：1px 描边，聚焦时主色描边 + 浅蓝环，舒适内边距。
- **卡片**：白底 1px 描边，分组承载控件，配字段标签。
- **状态栏**：底部，次要文字。
- **状态反馈**：加载（状态栏提示 + 按钮置灰）、空（去登录提示卡）、错误（友好弹窗）。

## 三个视图（结构不变，视觉统一）
1. **登录**：账号密码 / 扫码 两个标签页，卡片化居中布局。
2. **下载**：选科目 → 选讲 → 保存路径 → 下载，同屏完成；支持单/批量、预览、课程ID、导入导出。
3. **历史**：卡片化任务列表 + 续传 / 清空。

## 验收
代码实现后用 headless 方式真实构建窗口并遍历三视图，确认可启动、无报错、无 V1/V2 字样。
